# Keep the node_modules layout strict so the vite resolver's `/node_modules/<pkg>/`
# substring matching stays predictable (see vite/growi-react-resolver.ts).
shamefully-hoist=false
strict-peer-dependencies=false
